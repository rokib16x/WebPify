# Optional — required only for the global conversion counter (api/conversion-count.js).
# Create a free Redis database at https://upstash.com/ and paste the REST credentials.

UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here
